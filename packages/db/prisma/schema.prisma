generator client {
  provider = "prisma-client-js"
}
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Ticks {
  price      String
  symbol      String
  stream String
  tradeTime      BigInt
  tradeId      Int @unique
}